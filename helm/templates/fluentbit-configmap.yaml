{{- if .Values.fluentbit.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-fluentbit-configmap" (include "nginx.fullname" .) }}
  labels:
    {{- include "nginx.labels" . | nindent 4 }}
data:
  fluent-bit.conf: {{ tpl (.Files.Get "config/fluent-bit.conf") . | quote }}
  {{- if .Values.fluentbit.errorLog.enabled }}
  fluent-bit-parsers.conf: {{ tpl (.Files.Get "config/fluent-bit-parsers.conf") . | quote }}
  {{- end }}
{{- end }}
